# Claude Code 学习手册 — 互动升级方案

> 定位：面向会用 AI 编程但技术深度不高的学习者，不只是开发者
> 目标：从「枯燥看文档」变为「可交互的学习体验」
> 当前状态：多页面静态手册，内容深度已达标，交互形式单一

---

## 一、当前手册待优化项（先做）

### 1.1 内容拆分与深化

| 待办 | 现状 | 目标 |
|------|------|------|
| Buddy 独立为单独页面 | 嵌在 systems-overview 里 | 独立页 + 可交互精灵渲染 + 稀有度计算器 |
| 子系统拆分 | 20+ 系统在一个页面 | Voice、Vim、Remote、Bridge、Plugin、Memory 各自独立 |
| 架构图重做 | 静态 CSS 方块 | SVG 交互式节点图（可点击、连线动画） |
| 12 节课内容扩充 | 从旧合并页提取，部分内容单薄 | 每课独立万字级深度 + 对应到官方源码映射 |
| KAIROS 页补充 | 已有 1600+ 行 | 补充实际使用流程截图/模拟、与 OpenClaw 详细对比 |

### 1.2 视觉与交互

| 待办 | 说明 |
|------|------|
| 架构层级图升级 | 从扁平 `.layers` 方块 → SVG 带连线箭头的节点关系图 |
| Buddy 精灵实时渲染 | 用 JS 还原 sprites.ts 的渲染逻辑，输入 userId 实时生成 |
| 代码可视化 | 关键代码片段用动画逐行高亮执行流（如 agent_loop 的 while 循环） |
| 对比视图 | 课程间的「变更对比」用真正的 diff 视图而非静态表格 |
| 移动端优化 | 底部 Tab 栏导航、手势滑动切换章节 |

### 1.3 导航与体验

| 待办 | 说明 |
|------|------|
| 阅读进度追踪 | 每页底部进度条 + 首页显示已读/未读状态 |
| 全站搜索增强 | 当前只搜当前页标题，需要全站全文搜索 |
| 快捷键 | `j/k` 翻页、`/` 搜索、`[` `]` 上下章节 |
| 打印/导出优化 | 全站一键导出 PDF，保留排版 |

---

## 二、互动学习机制（内容优化后做）

### 2.1 Agent 循环沙盒模拟器

**核心理念：** 让学习者亲手操作 Agent 循环，而不是看别人描述

**交互设计：**

```
┌─────────────────────────────────────────────────────┐
│  Agent 循环模拟器                          [重置]    │
├─────────────┬───────────────────────────────────────┤
│             │                                       │
│  用户输入    │   Agent 大脑（可视化）                  │
│  ┌────────┐ │   ┌──────────────────────────────┐    │
│  │ 输入框  │ │   │  messages[] 实时增长可视化    │    │
│  │        │ │   │                              │    │
│  └────────┘ │   │  [user] "创建 hello.py"      │    │
│             │   │  [assistant] tool_use: bash   │    │
│  可用工具    │   │  [tool_result] "created"      │    │
│  ☑ bash    │   │  [assistant] "Done!"          │    │
│  ☑ read    │   │                              │    │
│  ☑ write   │   └──────────────────────────────┘    │
│  ☐ grep    │                                       │
│             │   当前状态                             │
│  控制面板    │   stop_reason: [end_turn ▾]           │
│  [下一步]   │   token count: 2,847                  │
│  [自动播放]  │   loop iteration: 3                   │
│  速度: ━━●━ │   tools called: 2                     │
│             │                                       │
├─────────────┴───────────────────────────────────────┤
│  时间线 ●───●───●───●───○───○                        │
│         1   2   3   4   5   6                       │
└─────────────────────────────────────────────────────┘
```

**场景预设（对应 12 节课）：**

| 课程 | 模拟器场景 | 学习目标 |
|------|-----------|---------|
| S01 | 只有 bash，执行 "创建文件" | 理解 while + stop_reason 基本循环 |
| S02 | 4 个工具可切换启停 | 理解 dispatch map，观察不同工具被选择 |
| S03 | 带 TodoManager 面板 | 观察 nag reminder 何时注入 |
| S04 | 主/子 agent 双面板 | 观察子 agent 的 messages[] 独立运行再合并 |
| S05 | system prompt 实时显示 token 消耗 | 对比全加载 vs 按需加载的 token 差异 |
| S06 | 自动触发压缩 | 观察 micro_compact → auto_compact 的过程 |
| S07 | 任务 DAG 可视化 | 拖拽创建依赖，观察 blockedBy 自动解锁 |
| S08 | 后台任务时间线 | 观察主线程继续 + 通知队列排空 |
| S09 | 多 agent 邮箱动画 | 观察 JSONL 收件箱的 send/read/drain |
| S10 | 协议握手动画 | shutdown request-response FSM 可视化 |
| S11 | 空闲轮询动画 | idle poll → 扫描任务板 → auto-claim |
| S12 | worktree 目录树 | 观察文件隔离 + 事件流 |

**技术方案：**
- 纯前端 JavaScript，无需后端
- 不调用真实 API，用预定义的响应序列模拟
- 每个步骤可暂停、回退、修改参数
- 响应式设计，手机上可用

---

### 2.2 闯关式知识检验

**核心理念：** 每学完一个概念，用一个必须通过的小关卡验证理解

**关卡类型设计：**

#### 类型 A：Bug 诊断
```
┌────────────────────────────────────────────┐
│  🔴 Bug Report                             │
│                                            │
│  Agent 陷入了死循环，已经跑了 50 轮。       │
│  以下是最后 3 轮的 messages：               │
│                                            │
│  [assistant] tool_use: bash                │
│  [tool_result] "Error: permission denied"  │
│  [assistant] tool_use: bash  ← 同样的命令   │
│  [tool_result] "Error: permission denied"  │
│  ...                                       │
│                                            │
│  问题出在哪里？                              │
│  ○ stop_reason 判断有误                     │
│  ○ 没有错误处理，相同命令无限重试             │
│  ● 权限被拒绝但 Agent 不知道换方案           │
│  ○ token 超出上下文窗口                      │
│                                            │
│                              [验证答案 →]   │
└────────────────────────────────────────────┘
```

#### 类型 B：拖拽排序
```
┌────────────────────────────────────────────┐
│  把 Agent 循环的步骤拖成正确顺序：           │
│                                            │
│  ┌──────────────────────┐                  │
│  │ 检查 stop_reason     │  ← 拖动我        │
│  └──────────────────────┘                  │
│  ┌──────────────────────┐                  │
│  │ 发送消息给 LLM       │  ← 拖动我        │
│  └──────────────────────┘                  │
│  ┌──────────────────────┐                  │
│  │ 执行工具调用          │  ← 拖动我        │
│  └──────────────────────┘                  │
│  ┌──────────────────────┐                  │
│  │ 追加 tool_result     │  ← 拖动我        │
│  └──────────────────────┘                  │
│                                            │
│                    [正确顺序：2→3→1→4→...]   │
└────────────────────────────────────────────┘
```

#### 类型 C：连线匹配
```
┌────────────────────────────────────────────┐
│  把左边的概念和右边的描述连起来：             │
│                                            │
│  Bones     ●────────────● LLM 生成，永久存储 │
│  Soul      ●────────────● 确定性，不存储     │
│  Shiny     ●────────────● 1% 概率触发       │
│  Rarity    ●────────────● 5 级权重轮盘      │
│                                            │
└────────────────────────────────────────────┘
```

#### 类型 D：填空计算
```
┌────────────────────────────────────────────┐
│  Buddy 属性计算：                            │
│                                            │
│  一只 Epic (floor=35) 宠物的峰值属性        │
│  计算公式: min(100, floor + 50 + random)    │
│                                            │
│  如果 random = 20，峰值 = [____]            │
│                                            │
│  输入: 100  ← (因为 min(100, 35+50+20=105)) │
│                              [验证 ✓]       │
└────────────────────────────────────────────┘
```

#### 类型 E：场景决策
```
┌────────────────────────────────────────────┐
│  你在一个敏感的生产仓库工作，需要：           │
│  1. 先让 Claude 研究代码结构                 │
│  2. 审批方案后再修改代码                      │
│  3. 不能自动执行危险命令                      │
│                                            │
│  应该使用哪种权限模式？                       │
│  ○ default                                 │
│  ○ acceptEdits                             │
│  ● plan                                    │
│  ○ auto                                    │
│  ○ bypassPermissions                       │
│                                            │
│  💡 答对了！plan 模式会让 Claude 先研究、     │
│  写方案，等你批准后才进入实施阶段。            │
└────────────────────────────────────────────┘
```

**完整关卡清单（每课 2-3 关）：**

| 课程 | 关卡 1 | 关卡 2 | 关卡 3 |
|------|--------|--------|--------|
| S01 Agent Loop | 排序：循环步骤 | Bug：死循环诊断 | 填空：stop_reason 值 |
| S02 Tool Use | 连线：工具→功能 | 场景：选择合适工具 | Bug：未知工具处理 |
| S03 TodoWrite | 场景：计划何时更新 | 填空：nag 触发条件 | Bug：两个 in_progress |
| S04 Subagent | 对比：主/子 token 消耗 | 场景：何时用 subagent | 排序：subagent 生命周期 |
| S05 Skills | 计算：token 节省量 | 场景：什么该做 skill | 连线：两层加载机制 |
| S06 Compact | 排序：三层压缩顺序 | 计算：压缩阈值 | 场景：信息丢失诊断 |
| S07 Task | 拖拽：构建 DAG | 场景：依赖解锁 | Bug：循环依赖 |
| S08 Background | 时间线：并行任务 | 场景：何时后台化 | Bug：通知未排空 |
| S09 Teams | 连线：消息类型 | 场景：团队 vs subagent | 排序：teammate 生命周期 |
| S10 Protocols | 场景：关机握手 | 排序：FSM 状态转换 | Bug：correlation ID 错配 |
| S11 Autonomous | 场景：idle vs work | 计算：超时时间 | Bug：身份丢失 |
| S12 Worktree | 拖拽：绑定任务和目录 | 场景：并行冲突 | 排序：事件流 |
| 架构 | 连线：层级关系 | 场景：请求路径 | 计算：启动优化 |
| 工具 | 分类：42 工具族 | 场景：权限模式 | 排序：工具调用链 |
| 命令 | 分类：prompt/local/jsx | 场景：看不到命令 | 连线：命令→类型 |
| KAIROS | 排序：激活流程 | 场景：Brief vs 普通回复 | 连线：feature flag |
| Buddy | 计算：稀有度概率 | 场景：Bones vs Soul | 连线：属性→数值范围 |

---

### 2.3 可视化架构探索器

**核心理念：** 用可交互的节点图替代静态分层方块

**设计草图：**

```
┌────────────────────────────────────────────────────────┐
│  Claude Code 架构探索器               [请求路径] [全局] │
│                                                        │
│           ┌──────────┐                                 │
│           │ User     │                                 │
│           │ Input    │                                 │
│           └────┬─────┘                                 │
│                │                                       │
│           ┌────▼─────┐        ┌──────────┐             │
│           │ Command  ├───────→│ Local UI │             │
│           │ Router   │        │ (config) │             │
│           └────┬─────┘        └──────────┘             │
│                │                                       │
│         ┌──────▼──────┐                                │
│         │ Context     │←── CLAUDE.md                   │
│         │ Builder     │←── Rules                       │
│         │             │←── Memory                      │
│         └──────┬──────┘←── Skills (names)              │
│                │                                       │
│         ┌──────▼──────┐     ┌────────────┐             │
│         │ Query       │────→│ Permission │             │
│         │ Engine      │←────│ Gate       │             │
│         │ (Tool Loop) │     └────────────┘             │
│         └──────┬──────┘                                │
│                │                                       │
│     ┌──────────┼──────────┐                            │
│     ▼          ▼          ▼                            │
│  ┌──────┐  ┌──────┐  ┌──────┐                         │
│  │ Bash │  │ Read │  │ MCP  │  ... 42 tools           │
│  └──────┘  └──────┘  └──────┘                         │
│                                                        │
│  ▸ 点击任意节点查看详情                                  │
│  ▸ 流动粒子显示数据流向                                  │
│  ▸ 切换视角：启动流程 / 工具调用 / 权限决策               │
└────────────────────────────────────────────────────────┘
```

**实现方案：**
- 使用 SVG 绘制节点和连线
- CSS 动画做流动粒子效果（沿 path 移动的圆点）
- 点击节点弹出侧边详情面板
- 3 种预设视角（一次请求路径 / 启动流程 / 权限决策链）
- 缩放和拖拽支持（CSS transform）
- 纯前端，无依赖

---

### 2.4 概念闪卡 + 间隔重复

**核心理念：** 把每章核心知识点变成可翻转的卡片，支持自评和定时复习

**交互设计：**

```
┌────────────────────────────────────────────┐
│  📝 概念卡片  S01: Agent Loop    3/12      │
├────────────────────────────────────────────┤
│                                            │
│                                            │
│    Agent 循环的唯一退出条件是什么？          │
│                                            │
│                                            │
│              [翻转查看答案]                  │
│                                            │
├────────────────────────────────────────────┤
│  [😰 不懂]    [🤔 模糊]    [✅ 掌握]       │
└────────────────────────────────────────────┘

翻转后：

┌────────────────────────────────────────────┐
│                                            │
│  stop_reason !== "tool_use"                │
│                                            │
│  当模型返回的 stop_reason 不是 "tool_use"   │
│  时（通常是 "end_turn"），循环结束。         │
│  这意味着模型认为任务已完成，不再需要调用    │
│  任何工具。                                  │
│                                            │
├────────────────────────────────────────────┤
│  [😰 不懂]    [🤔 模糊]    [✅ 掌握]       │
│  (1分钟后复习) (10分钟后)   (明天复习)       │
└────────────────────────────────────────────┘
```

**卡片数据规模：**

| 章节 | 卡片数 | 核心考点 |
|------|--------|---------|
| S01 | 8 张 | 循环结构、stop_reason、messages 累积 |
| S02 | 10 张 | dispatch map、safe_path、工具注册 |
| S03 | 8 张 | TodoManager、in_progress 约束、nag |
| S04 | 8 张 | 上下文隔离、messages=[]、摘要返回 |
| S05 | 8 张 | 两层加载、token 成本、SKILL.md 格式 |
| S06 | 10 张 | 三层压缩、阈值计算、transcripts |
| S07 | 10 张 | 任务 DAG、blockedBy、持久化 |
| S08 | 8 张 | 守护线程、通知队列、drain |
| S09 | 10 张 | MessageBus、JSONL、5 种消息类型 |
| S10 | 8 张 | FSM、correlation ID、握手协议 |
| S11 | 10 张 | idle poll、auto-claim、身份重注入 |
| S12 | 10 张 | worktree、事件流、崩溃恢复 |
| 架构 | 12 张 | 启动流程、QueryEngine、状态管理 |
| 工具 | 15 张 | 42 工具分类、权限模型、调度 |
| 命令 | 10 张 | 3 种类型、过滤链、67 命令 |
| KAIROS | 12 张 | Feature Flag、Tick、Brief、Coordinator |
| Buddy | 10 张 | Bones/Soul、Mulberry32、稀有度 |
| 权限 | 10 张 | 6 模式、classifier、hooks |
| **总计** | **~180 张** | |

**技术方案：**
- 卡片数据以 JSON 存储在每个页面
- 间隔重复算法（简化版 SM-2）存 localStorage
- 首页显示「今日待复习 N 张」
- 支持筛选章节

---

### 2.5 AI 导师对话（Phase 2）

**核心理念：** 在每个页面嵌入一个理解当前章节内容的 AI 对话框

**交互设计：**

```
┌────────────────────────────────────────────┐
│  💬 Ask about this chapter                 │
├────────────────────────────────────────────┤
│                                            │
│  你: Bones 为什么不存储？                    │
│                                            │
│  导师: 好问题！让我反问你——如果 Bones        │
│  存储在配置文件里，一个用户想把自己的宠物    │
│  从 Common 改成 Legendary，他需要做什么？    │
│                                            │
│  你: 直接改配置文件的 rarity 字段？          │
│                                            │
│  导师: 没错！这就是不存储 Bones 的第一个     │
│  原因——防作弊。配置文件里根本没有 rarity     │
│  字段可以改。那第二个原因呢？如果开发者      │
│  把 "capybara" 改名为 "big_hamster"...      │
│                                            │
│  ┌──────────────────────────────────┐      │
│  │ 输入你的问题...                   │      │
│  └──────────────────────────────────┘      │
└────────────────────────────────────────────┘
```

**实现方案：**
- 方案 A：接入 Claude API（需要 API Key，有成本）
- 方案 B：接入免费的 Claude.ai Web（需要用户登录）
- 方案 C：纯前端预设 FAQ 树（无 AI，但零成本）
- 建议 Phase 1 用方案 C，Phase 2 升级到 A

**每章预设 FAQ 示例（S01）：**
- "什么是 stop_reason？" → 解释 + 示例
- "为什么需要循环？不能一次调用搞定吗？" → 多步任务解释
- "messages 数组会无限增长吗？" → 引导到 S06 上下文压缩
- "这跟 ChatGPT 有什么区别？" → Agent vs Chat 对比

---

## 三、Buddy 精灵互动专页

**独立页面：** `pages/buddy-interactive.html`

**功能设计：**

### 3.1 精灵生成器
```
┌────────────────────────────────────────────┐
│  🎰 Buddy 生成器                            │
│                                            │
│  输入你的 User ID: [________________]       │
│                     [生成我的 Buddy →]       │
│                                            │
│  ┌──────────────────────────────────┐      │
│  │         \^^^/                    │      │
│  │          __                      │      │
│  │        <(✦)___                   │      │
│  │         (  ._>                   │      │
│  │          `--´                    │      │
│  │                                  │      │
│  │  名字: Noodle                    │      │
│  │  物种: Duck 🦆                   │      │
│  │  稀有度: ★★★ Rare (10%)         │      │
│  │  帽子: Crown 👑                  │      │
│  │  眼睛: ✦                        │      │
│  │  闪光: No                        │      │
│  │                                  │      │
│  │  属性:                            │      │
│  │  DEBUGGING ████████████░░░ 78    │      │
│  │  PATIENCE  ██████░░░░░░░░ 42    │      │
│  │  CHAOS     ████████████████ 95 ← 峰值 │
│  │  WISDOM    █████████░░░░░░ 61    │      │
│  │  SNARK     ███░░░░░░░░░░░ 23 ← 垃圾  │
│  └──────────────────────────────────┘      │
│                                            │
│  [撸一下 ♥]  [看动画]  [换个 ID]            │
└────────────────────────────────────────────┘
```

### 3.2 18 物种画廊
- 每个物种的 3 帧动画循环展示
- 点击查看 face 表情和 hat 变体
- 标注哪个物种名触发了安全扫描加密

### 3.3 稀有度计算器
- 输入任意字符串 → 实时计算 hash → seed → rarity
- 展示 Mulberry32 PRNG 的每一步中间值
- 可视化权重轮盘（饼图 + 指针动画）

### 3.4 属性模拟器
- 选择稀有度 → 随机生成一组属性
- 对比 Common vs Legendary 的属性分布
- 柱状图动画展示 floor 差异

---

## 四、实施路线图

### Phase 1：内容深化（1-2 周）

```
Week 1:
├── Buddy 拆分为独立页 + 精灵渲染
├── 子系统页面拆分（Voice/Vim/Remote/Bridge/Plugin/Memory 各自独立）
├── 架构图升级为 SVG 交互式
└── 12 节课内容补充完整

Week 2:
├── KAIROS 页面完善
├── 全站搜索升级为全文搜索
├── 阅读进度追踪
└── 移动端体验优化
```

### Phase 2：互动机制（2-3 周）

```
Week 3:
├── Agent 循环沙盒模拟器（S01 场景）
├── 闯关关卡系统框架 + S01-S03 关卡
└── 概念闪卡系统框架 + S01-S06 卡片

Week 4:
├── 模拟器扩展到 S02-S06 场景
├── S04-S12 关卡
├── S07-S12 + 架构/工具/命令/KAIROS/Buddy 卡片
└── Buddy 精灵互动专页

Week 5:
├── 可视化架构探索器
├── 闯关进度系统（解锁下一章）
└── 整体测试和优化
```

### Phase 3：AI 增强（后续）

```
├── AI 导师对话（接入 Claude API）
├── 智能推荐学习路径
├── 根据闯关表现调整内容难度
└── 社区功能（分享闯关成绩）
```

---

## 五、技术架构

### 当前技术栈（保持）
- 纯静态 HTML/CSS/JS
- 无构建工具、无框架依赖
- `file://` 协议可直接打开
- localStorage 持久化用户状态

### 新增技术需求

| 功能 | 技术方案 | 依赖 |
|------|---------|------|
| Agent 模拟器 | 纯 JS 状态机 + DOM 操作 | 无 |
| 闯关系统 | HTML 模板 + JS 验证逻辑 | 无 |
| 概念闪卡 | JS + localStorage (SM-2 算法) | 无 |
| SVG 架构图 | 内联 SVG + CSS 动画 | 无 |
| Buddy 精灵渲染 | 还原 sprites.ts 的 JS 逻辑 | 无 |
| 全文搜索 | 预构建的搜索索引 JSON | 无 |
| AI 导师 | Claude API / 预设 FAQ | Phase 3 才需要 API |

**所有 Phase 1-2 功能都是零依赖、纯前端实现。**

---

## 六、成功指标

| 指标 | 当前 | 目标 |
|------|------|------|
| 平均页面停留时间 | 无法衡量 | > 5 分钟/页 |
| 章节完成率 | 无追踪 | > 60% 读完全部章节 |
| 闯关通过率 | 不存在 | > 70% 首次通过 |
| 闪卡复习率 | 不存在 | > 40% 用户使用 |
| 模拟器使用率 | 不存在 | > 50% 用户与模拟器交互 |
| Buddy 生成器 | 不存在 | > 80% 用户尝试生成 |

---

## 七、设计原则

1. **渐进复杂度** — 从简单到复杂，每一步都在前一步的基础上加一点东西（与 learn-claude-code 12 节课的设计哲学一致）
2. **先做后学** — 让用户先与模拟器互动产生直觉，再阅读文字解释原理
3. **无门槛** — 不需要装任何东西、不需要 API Key、不需要写代码。打开浏览器就能学
4. **不打断** — 互动元素嵌入文档流中，不是弹窗或强制跳转。想深入就深入，想跳过就跳过
5. **真实感** — 模拟器的行为尽可能还原真实 Claude Code 的行为模式，不是玩具级简化

---

*文档版本: v1.0*
*创建日期: 2026-04-02*
*状态: 方案设计阶段，待审核后按优先级实施*
